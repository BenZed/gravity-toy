{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        // Build Options
        "build": false, // we'll never need to build test fields
        "target": "esnext", // prevents jest tests from failing,
        "types": [
            "node",
            "jest"
        ]
    }
}